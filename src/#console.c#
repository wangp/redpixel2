/* console.c
 *
 * Peter Wang <tjaden@users.sourceforge.net>
 */


#include <allegro.h>


#define MAX_LINES	100

static char *line[MAX_LINES];
static int num_lines;


int console_init ()
{
    num_lines = 0;
    return 0;
}


void console_shutdown ()
{
    for (i = 0; i < MAX_LINES; i++)
	if (line[i])
	    free (l
}
