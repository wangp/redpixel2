# hacked from fireball.egg

type fragment
{
   size := 12;
   aa := 1;

   r := 255;
   g := 200;
   b := 210;

   dx := 2*(rand-0.5);
   dy := 2*(rand-0.5);

   x = x+dx;
   y = y+dy;

   r = r*0.93;
   g = g*0.9;
   b = b*0.8;

   r = r*0.95;
   g = g*0.95;
   b = b*0.95;
}

lay (48) fragment;

